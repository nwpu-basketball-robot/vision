#include "findvolleyball.h"
#include <boost/shared_ptr.hpp>
#include <boost/make_shared.hpp>
using namespace boost ;

int main(int argc , char **argv)
{
    ros::init(argc,argv,"ballFind") ;
        ros::NodeHandle node ;
        SimpleOpenNIViewer ball_find(node) ;
        return 0 ;
}
